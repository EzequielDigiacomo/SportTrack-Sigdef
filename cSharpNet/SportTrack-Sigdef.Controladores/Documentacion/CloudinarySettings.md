# CloudinarySettings

**Archivo fuente:** `SportTrack-Sigdef.Controladores/Documentacion/CloudinarySettings.cs`

## 1. Qué es este archivo

Es un **Clase de configuración** de la capa `SportTrack-Sigdef.Controladores` (servicios, repositorios y DTOs).

## 2. Conceptos C# / .NET que aparecen

- **Options pattern / Settings**: clases de configuración enlazadas a `appsettings.json`.

## 3. Namespace y usings

- **Namespace:** `SportTrack_Sigdef.Controladores.Documentacion`
- **Usings:**
  - _(ninguno explícito)_

## 4. Detalle del tipo — tipo principal

### Propiedades

- `CloudName` (`string`)
- `ApiKey` (`string`)
- `ApiSecret` (`string`)

## 5. Notas de estudio

- Leé el `.cs` junto a este `.md` y marcá cada `using` e interfaz inyectada hasta entender el grafo de dependencias.
- Ruta relativa en el proyecto: `Documentacion/CloudinarySettings.cs`.

---

*Documentación generada para aprendizaje C#/.NET a partir del código fuente real.*
